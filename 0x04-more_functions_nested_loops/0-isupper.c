#include "main.h"

/**
 * _isupper - Check if a letter is upper
 * @c: The number to be checked
 * Return : 1 if c is capital, otherwise 0
 */

int _isupper(int c)

{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
return (0);
}
